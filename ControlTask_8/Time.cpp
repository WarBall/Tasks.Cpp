#include "TimeClass.h"

/* 
_seconds = _seconds > 59 ? _seconds - 60, _minutes++ : _seconds;
_minutes = _minutes > 59 ? _minutes - 60, _hours++ : _minutes;
_hours = _hours > 23 ? 23 : _hours; 

std::cout << _hours << ":" << _minutes << ":" << _seconds;
*/

